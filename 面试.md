自我介绍
你好, 我叫荆冬冬,我是有将近5年的前端开发经验,主要是使用vue相关的技术栈,一直在做一些

你好, 我叫荆冬冬, 我在上一家正式单位一直是在中医事业部, 所以一直在做医疗相关的项目
我们之前做的项目就是各家医院首先可以接入我们的循证分析平台, 然后可以创建课题, 提交模版定制化服务吗
然后模版定制之后就可以进行病历采集, 采集的数据会集中到中医数据仓库这个系统进行数据审核清洗双录入等流程操作
之后各家医院可以在循证分析平台上选择数据类型和算法进行数据算法分析, 比如数据配伍, 单样本t检验, 卡方计算之类的
然后最终产出计算报表, 基本上就是整个体系的核心业务, 目的为课题研究和论文提供数据支持的

然后现在这家单位是远程的, 现在的任务就是把整个c端的所有功能全部用b端架构实现
其中就包含病历录入, 模版设计之类的功能, 其他的功能基本上都是单表维护, 大概就是这些内容

## 说说预处理和原子化css

预处理器的话有点就是可以用变量,函数,嵌套,循环等,比直接写css要方便很多,像主题系统大部分都是用变量来切换不同的主题样式

原子化css我的理解就是 样式复用的极致和自由的编排能力,他的颗粒度非常小,非常灵活,不用纠结类名,减少一些心智负担,可以减少 CSS 体积,他是支持tree-shaking的,可以只打包使用到的样式,熟练的话可以提升开发效率,不需要在html和css模块中来回切换


## vue响应式原理
Vue 响应式的核心是「数据劫持 + 依赖收集 + 触发更新」也就是数据劫持和发布订阅模式的结合

1. Vue2 基于 Object.defineProperty 劫持对象属性的 get/set：初始化时遍历 data 所有属性，在 get 中收集依赖（Watcher），set 中触发依赖更新；但它只能监听已有属性，无法检测对象新增 / 删除、数组索引修改，需通过 $set 兜底。

2. Vue3 改用 Proxy 代理整个对象，结合 Reflect 保证操作规范：天然支持对象新增 / 删除属性、数组索引修改，嵌套对象采用懒代理（访问时才代理），性能更优；同时通过 ref 解决基本类型响应式问题，提供 shallowReactive 等细粒度 API 适配不同场景。